extends Viewport


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	set_size_override(true, Vector2(700, 700)) # Custom size for 2D.
	set_size_override_stretch(true) # Enable stretch for custom size.
#	pass
